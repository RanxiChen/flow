# r7 SD initialization capture, 2026-09-12

Loaded image reported by user: single SD/coherent DMA/ILA r7, Flow 29b8f53.
Live KU040 ILA was reached through Alan localhost:3121, without programming
or resetting the FPGA. Captures are in the r7 build's ila-sd-live/ directory.

- capture.vcd: 4096 system-clock samples, 496 retires; BIOS polls
  sdcard_wait_cmd_done at 0x10016998..0x100169ba. MMIO acknowledges; no
  D-cache/MMIO error and no active DMA request. cmd_event stays zero.
- command2.vcd: CMD0 argument=0, command=0, send=1. Before this command,
  cmd_event=5 (done+timeout). This does not identify the previous command alone.
- cmd8-start.vcd: argument=0x1aa, command=0x805, send=1. Prior event=1.
  Only the start of the frame fits the 40.96 us capture, not the full response.
- card-response3.log: AND trigger cmd_oe=0, cmd_i=0, sample_ce=1 armed at
  21:07:06. No trigger was reported before the batch's 45-second overall
  timeout (includes startup, so this is not 45 seconds of armed observation).
  This observes FPGA input, not the card connector. It does not prove mux state.

UG917 v1.10 Figure 1-7: U107 and U110 select system controller at
SYSCTLR_SDIO_MUX_SEL=0, FPGA at 1. SW15.6 selects configuration mode and
is not direct evidence of SD mux ownership. Installed system-controller v1.0
responds on ttyUSB0; Get GPIO PL Data does not expose SD mux state. Returned
its menu to Main Menu. User's BIOS ttyUSB1 was not opened or written.

Next hardware discrimination needs the actual mux select or card-side CMD/CLK
observation. Do not infer card-side activity from FPGA-side output loopback.
No card writes, FPGA reset, or FPGA configuration were performed.

Software correction: limit initial CMD0/CMD8 retry loop to 8 attempts (was
1000), printing CMD8 error and attempt count. This improves bounded failure
reporting, not card connectivity. Alan RV64 compiler syntax check passed using
r7 generated headers. The loaded ROM remains unchanged; no new bitstream was
built for this correction.
